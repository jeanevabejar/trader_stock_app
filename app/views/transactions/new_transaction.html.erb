<%= turbo_frame_tag "modal" do%>
<div class="modal">
<%= render 'form' %>
<%= link_to "Cancel", pages_user_path, data: {
        controller: "modals",
        action: "modals#close"
      }, class: "cancel-button" %>
</div>

<% end %>
